# Redux
 - Store
 - Action
 - Reducer
 - Subscription
    
    ```
    store.subscribe(() => {
        console.log('[subscription]', store.getState())
    })
    ```
 - Selectors

# Redux-toolkit
 - configureStore
 - createAction
 - createReducer
 - createSlcie

    create actions and reducer